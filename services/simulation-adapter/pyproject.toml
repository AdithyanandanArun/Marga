[project]
name = "marga-simulation-adapter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "asyncio>=3.4",
]
[project.optional-dependencies]
traci = ["traci>=1.20"]
libsumo = ["libsumo>=1.20"]
